torch>=2.0.0
torchvision>=0.15.0
# basicsr must be installed separately — see README (known build issue on Python 3.13+)
# pip install git+https://github.com/XPixelGroup/BasicSR.git --no-build-isolation
realesrgan>=0.3.0
facexlib>=0.3.0
gfpgan>=1.3.8
opencv-python>=4.5.0
Pillow>=9.0.0
tqdm>=4.60.0
einops>=0.6.0
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
requests>=2.28.0
